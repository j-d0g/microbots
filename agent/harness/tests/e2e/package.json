{
  "name": "harness-e2e-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic Playwright e2e for the V1 builder flow (Agent C).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
